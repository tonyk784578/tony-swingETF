# 사전 등록 초안: overnight — 장중 모멘텀의 야간 지속 (오버나이트 프리미엄)

상태: **초안 v0 (2026-08-26) — 백테스트 미실행, 결과 미관측.**
승인 후 이 문서의 내용을 config.yaml `etf.strategies.overnight` 주석으로 옮겨
등록을 확정하고, **실행 전에 커밋**한다 (등록이 결과보다 먼저임을 git이 증명).
승인 전까지 어떤 수치도 계산하지 않는다.

## 1. 가설 (메커니즘)

주식 수익의 상당 부분이 장중이 아니라 **밤사이(당일 종가 → 익일 시가)에 발생**하며,
특히 **당일 장중에 강했던 종목일수록 야간 수익이 크다** (모멘텀의 야간 지속).

- 근거 문헌: Cliff, Cooper & Gulen (2008) "Return Differences between Trading and
  Non-Trading Hours: Like Night and Day" — 야간/주간 수익 비대칭.
  Berkman, Koch, Tuttle & Zhang (2012, JFQA) "Paying Attention: Overnight Returns
  and the Hidden Cost of Buying at the Open" — 개장 직후 주의(attention) 기반
  매수세가 야간 수익을 만들고 시가에서 소멸. Lou, Polk & Skouras (2019, JFE) —
  모멘텀 수익이 야간에 집중됨을 미 시장에서 확인. 국내 퀀트 커뮤니티에서도
  '오버나이트'(종가 매수 → 익일 시가 매도)는 널리 알려진 관례 전략이다.
- **왜 익일 시가에 파는가**: Berkman 등의 메커니즘 — 야간에 쌓인 관심 매수가
  개장 동시호가에 집중되어 시가가 고평가되기 쉽다. 시가 매도는 그 수요에
  파는 행위다. (본 프로젝트의 독립 증거와도 정합: us_dip/ewy 기각 = "야간 정보는
  시가에 이미 반영" — 시가에 사는 쪽은 엣지가 없었고, 그 반대편이 본 가설이다.)

## 2. 기존 계열과의 관계 (변형이 아님의 근거 + 등록된 중복 우려)

- 정보 집합: **당일 장중 가격**(시가 대비 종가) — 전일 종가 신호(trend/tom)와도,
  장중 돌파 트리거(volbreak)와도 다르다. 진입 시점(종가 동시호가)이 기존 계열에
  없던 유형이다.
- **등록된 우려(중복)**: volbreak도 야간을 보유한다(장중 진입 → 익일 시가 청산).
  두 전략의 보유 창이 겹치므로 상관이 높을 수 있다. 리포트에 (a) 신호일 중복률,
  (b) 두 슬리브 일간 수익 상관을 반드시 병기하고, **상관 |ρ|>0.5면 운영상 같은
  리스크 그룹으로 취급**(동시 노출 합산 캡)을 등록 시점에 약속한다.
  volbreak과 다른 점: volbreak은 '시가+0.5x전일폭 돌파'라는 경로 조건이고, 본
  전략은 종가 시점의 상태 조건 — 돌파 없이 꾸준히 오른 날을 포착한다.
- 종결 계열(평균회귀·미국야간신호·캘린더)과 메커니즘 무관.

## 3. 규칙 정의 (전 항목 고정 — 그리드서치 없음)

- **진입**: 한국 거래일 D의 **장중 수익률 = 종가/시가 - 1 > 0** 이면 D 종가 매수
  (동시호가 근사). 컷은 0 하나 — 부호 컷은 자연 상수이지 튜닝 파라미터가 아니다.
  다른 컷(+1% 등) 시험 금지.
- **청산**: D+1 시가 매도 (동시호가 근사). 보유 1일 고정, 연장 없음.
- **비용**: 왕복 0.1% (하우스 표준) + 판정 시 ETF별 실측 비용표 max 규칙.
- **유니버스**: 기본 12종 전부 (etf.universe — 제한·선별 없음). 12테스트.
- **데이터**: 일봉 OHLC만 사용 (분봉 불필요). D+1 시가가 없으면(미완성) 미계상.
- **룩어헤드 점검**: 신호는 D 종가 확정 시점의 정보만 사용. 라이브 구현은
  15:20 동시호가 직전 현재가로 근사하므로 종가 근처 ±0 경계에서 백테스트와
  어긋날 수 있음 — 이 오차는 섀도 단계의 preview 플래그로 실측한다 (등록 주의).

## 4. 게이트 (하우스 표준 — 변경 없음)

후보별: N>=30, 전반부(~2020)/후반부(2021~) 평균 모두 양수, t>=2.
추가로 **일 단위 교정 t를 병기** — 12종이 같은 날 동시 신호가 나는 구조라
트레이드 풀 나이브 t는 부풀려진다 (volbreak에서 3.7배 인플레 실측 선례).

**등록된 회의론 (실행 전 명시)**: 신호 빈도가 연 ~120건/종으로 예상되는 고회전
전략이라 **0.1% 왕복 비용이 야간 엣지(문헌상 수 bp/일)를 지울 가능성이 높다.**
게이트 실패가 예상 시나리오이며, 실패 시 그대로 기록하고 종결한다(컷 변경 재시험
금지). 이 전략의 가치는 통과 시에만 있고, 기각도 '한국 ETF 야간 프리미엄은 비용을
못 이긴다'는 지식으로 기록된다.

## 5. 홀드아웃 확인 (게이트 통과분에 한해 — 사전 등록)

게이트 통과 후보가 있으면, **같은 실행에서 1회** 홀드아웃 구간(2009-04~2014-12,
data/holdout_*.parquet 관례 — 캐시 없는 코드는 동일 역산 방식으로 신규 수집)에
동일 규칙을 적용해 방향(평균 부호)을 확인한다. 야간 프리미엄 문헌은 2010년대
이전이 더 강했다고 보고하므로, **홀드아웃 음수면 Stage 2 편입 보류**(우연 통과
가능성 우세로 기록). 이는 trend/tom이 홀드아웃을 나중에야 받은 교훈의 반영이다.

## 6. 시험 수 회계

12테스트 (12종 x 조건 1). 실행 시 `multiple_testing.declared_trials` 144 → 156.
홀드아웃 확인은 동일 규칙의 재현 시험이라 가산하지 않는다 (2026-08-14 관례).

## 7. 통과 시 Stage 2 / 판정 설계 (미리보기)

- 편입: 통과 후보를 etf_paper.candidates에 freeze 날짜와 함께 등록,
  `family: overnight`. 포트폴리오 시뮬 미편입 (1일 회전 별도 슬리브 —
  volbreak/rotation2 선례). 프리뷰는 "오늘 장중 +면 종가 매수" 안내만.
- 판정: 동시 신호 고상관 구조이므로 **후보별 개별 판정** (volbreak 선례).
  표본 수·Bonferroni 임계는 통과 후보 수가 확정되는 편입 시점에 검정력 계산과
  함께 못 박는다 (volbreak 등록 절차와 동일).

## 8. 금지 사항

- 컷 변경(+1%/-1% 등)·유니버스 선별·보유일 연장 등 어떤 변형 재시험도 금지.
- 결과 관측 후 이 문서 수정 금지 (오탈자 제외 — 수정은 git 이력에 남긴다).
- 실행은 승인 후 1회. 재실행은 리포트 재생성 용도로만.
