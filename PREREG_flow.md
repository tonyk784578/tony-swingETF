# 사전 등록: flow — 외국인 수급 추종 (2026-08-26)

상태: **등록 v1 (2026-08-26) — 백테스트 미실행, 결과 미관측.**
overnight(PREREG_overnight.md) 관례를 따른다: 이 문서 + config 등록 + 엔진을
**백테스트 실행 전에 커밋**해 등록과 결과의 선후를 git이 증명한다.

## 1. 가설 (메커니즘)

한국 시장에서 **외국인 순매수는 지속성이 있고 단기 수익률을 선행**한다.
외국인이 주 단위로 순매수 기조로 전환하면, 그 흐름이 며칠~수 주 이어지며
같은 시장의 주가지수를 밀어올린다.

- 근거 문헌: Choe, Kho & Stulz (1999, JFE) — 한국 시장 외국인의 양(+)의
  피드백 거래와 flow 지속성. Froot, O'Connell & Seasholes (2001, JFE) —
  신흥시장 외국인 자금 유입이 이후 수익률을 예측. Richards (2005, JFQA) —
  아시아 6개 시장에서 외국인 순매수의 가격 영향과 지속성. 국내 실무·연구에서
  '외국인 수급'은 가장 널리 인용되는 시장 방향 지표다.
- **본 프로젝트 맥락**: 수급은 이 프로젝트가 한 번도 시험하지 않은 정보 축이다
  (지금까지 가격·달력·야간·교차상장만 시험). "소급 가능 데이터는 새 가설 등록
  시점에 받는다"는 2026-08-11 원칙의 첫 적용 사례.

## 2. 규칙 정의 (전 항목 고정 — 그리드서치 없음)

- **신호 데이터**: 해당 시장(KOSPI/KOSDAQ)의 일별 **외국인 순매매 대금**(억원).
- **진입**: 외국인 순매매의 **5거래일 롤링합**이 음(<=0)에서 양(>0)으로 전환
  (양전 크로스)한 날 D → **D+1 시가 매수**. 창 5일 = 1주(자연 달력 단위 —
  tom·trend_ride의 20/60 관례와 같은 성격, 튜닝 아님). 다른 창 시험 금지.
- **청산**: 롤링합(D-1) < 0 이면 D 종가 청산 (**의도적 1일 지연** — D 종가
  시점에는 D의 확정 수급을 알 수 없다. 가격 전략과 달리 수급은 장 마감 후
  확정되므로 exit도 전일 정보만 쓴다). 최대 보유 10일 (하우스 기본 캡).
- **비용**: 왕복 0.1% (하우스 표준) + 판정 시 실측 비용표 max 규칙.
- **유니버스 (메커니즘 고정 — 성과 미참조)**: 시장 수급 → 해당 시장 지수 ETF만.
  - KOSPI 수급 → KODEX200(069500), KODEX_Lev(122630)
  - KOSDAQ 수급 → KODEX_KOSDAQ150(229200), KODEX_KOSDAQ150Lev(233740)
  - 4테스트. Inverse 제외(매수 방향 전략), 섹터 ETF 제외(시장 수급과 매핑 불순
    — 섹터별 수급은 별도 데이터라 시험 수만 늘린다).

## 3. 룩어헤드 시점 정의 (수급 데이터 특유 — 등록 시 확정)

- 수급(D)은 D **장 마감 후 확정** (거래소 발표). 백테스트·라이브 모두:
  - 진입 플래그(D+1) ← 수급(D)까지의 롤링합 (저녁 cron이 D 수급 수집 →
    다음날 아침 프리뷰가 신호 표시. 가격 전략의 shift(1)과 동일 구조)
  - 청산 플래그(D) ← 수급(D-1)까지 (위의 1일 지연 규칙)
- **장중 잠정치 오염 방지**: 공급 페이지는 당일 장중에도 잠정 행을 노출한다.
  로더가 `confirmed_cutoff()`(16시) 이전의 당일 행을 버린다 — 스냅샷
  premarket 방어(08-14)와 같은 계층의 방어.
- 잠정→확정 정정: 매 갱신 시 최근 5영업일 창을 재수집해 덮어쓴다
  (그보다 오래된 캐시는 불변 — 가격 캐시의 정정 감시 관례와 정합).

## 4. 데이터 공급선 (등록 시 확정 — 실측 근거)

- **1차 시도 KRX 공식(pykrx)은 불가**: 2026-08 실측 — KRX 정보데이터시스템이
  익명 API를 차단(응답 "LOGOUT"), pykrx 1.2.8은 로그인 자격증명을 요구.
  계정 발급 시 공급선 교체 가능(신호 정의 불변 — 같은 KRX 원천 수치).
- **채택: 네이버 금융 투자자별 매매동향(일별)** — 공개 페이지, KRX/코스콤
  원천의 확정치(억원). 실측: 2009년 이후 일별 제공, KOSPI/KOSDAQ 모두.
  캐시 `data/flow_{market}.parquet` (2009-01-01부터 — 홀드아웃 구간 포함
  단일 캐시. 가격과 달리 소스가 같아 라이브/홀드아웃 분리가 불필요).
- **공급 단절 내성 (스크레이핑 리스크의 정직한 평가)**: 수급은 **소급 수집
  가능**하므로 공급이 끊겨도 리플레이 장부는 무손실 — 프리뷰 신호만 늦어진다
  (preview 플래그가 이를 기록). 페이지 구조 변경은 파서 실패 → 캐시 degrade +
  [warn] 로그로 드러난다. Stage 2 편입 시 health에 수급 신선도 감시를 추가한다.

## 5. 게이트·진단·홀드아웃 (overnight 관례)

- 게이트: 하우스 표준 (N>=30, 전/후반 양수, t>=2), 후보별.
- 병기 진단 (results/flow_screening.md):
  1. 풀 나이브 t vs **일 단위 교정 t** — 같은 시장 페어(200/Lev)는 신호가
     동일해 트레이드 풀 t가 부풀기 때문 (volbreak/overnight 선례).
  2. **가격 모멘텀 중복 진단** — 외국인 순매수는 가격과 동시상관이 있어 수급
     신호가 사실상 breakout/macross와 겹칠 수 있다 (등록된 우려 1). 같은 코드
     trend 후보(breakout 등)와의 트레이드 보유일 중복률·슬리브 상관 병기.
     |ρ|>0.5 면 운영상 같은 리스크 그룹 취급.
- **홀드아웃 확인 (통과분 한정, 같은 실행에서 1회)**: 2009~2014에 동일 규칙
  적용, 평균 부호 확인 — 음수면 Stage 2 편입 보류. **KOSDAQ150 계열 2종은
  2015년 상장이라 홀드아웃 불가**를 등록 시점에 명시 — 통과해도 홀드아웃
  근거 없이 편입 여부를 결정해야 하며, 이 경우 '확인 불가'로 기록하고
  보수적으로 **편입 보류**한다 (tom의 교훈 — 독립 재현 없는 단독 통과는 약함).
  KOSPI 계열(069500/122630)은 가격 홀드아웃 캐시가 있어 확인 가능.

## 6. 시험 수 회계

4테스트 (4 ETF x 규칙 1). 실행 시 `multiple_testing.declared_trials` 156 → 160.
홀드아웃·진단은 동결 규칙의 재현/측정이라 가산하지 않는다.

## 7. 통과 시 Stage 2 / 판정 설계 (미리보기)

- 편입: 통과 + 홀드아웃 양수(가능한 후보) → etf_paper.candidates에
  `family: flow`, freeze 2026-08-26(실행일). 포트폴리오 시뮬 편입 여부는
  보유 수일 스윙이라 trend와 같은 구조지만, **일단 미편입**으로 등록하고
  판정 통과 후 재검토 (수급 신호의 독립성 확인이 먼저).
- 판정: 표본 수·검정(같은 시장 페어의 동시 신호 구조 반영)·검정력은 편입
  시점에 등록한다 (overnight 관례 — 빈도를 본 뒤에만 계산 가능).
- 프리뷰: 아침에 "어제까지 수급 기준 오늘 시가 진입" 표시 (가격 전략과 동일
  구조 — volbreak/overnight 같은 특수 안내 불필요).

## 8. 금지 사항

- 창(5일)·컷(0)·유니버스·보유 캡 변형 재시험 금지. 기관/개인 수급으로의
  교체 시험 금지 (그건 새 가설로만).
- 결과 관측 후 이 문서 수정 금지 (오탈자 제외).
- 실행은 1회. 재실행은 리포트 재생성 용도로만.
