# 사전 부검 (Pre-mortem) — 미래의 나에게 보내는 커밋먼트 (2026-08-26)

이 프로그램의 최대 실패 모드는 통계가 아니라 **관측 도중 운영자가 자기 규칙을
깨는 것**이다. 아래는 일어날 법한 시나리오와, 데이터를 보기 전인 지금 미리
정해 두는 대응이다. 시나리오가 실제로 닥쳤을 때 이 문서를 열어라 — 그 순간의
판단력은 지금보다 나쁘다 (손실 구간의 의사결정은 체계적으로 성급하다).

## 시나리오와 사전 결정

**S1. 섀도 누적이 크게 마이너스다 (예: volbreak -30%, 1년 경과)**
→ 아무것도 바꾸지 않는다. 판정 규칙(300건 t검정)이 결정하지 감정이 결정하지
않는다. 인샘플 sd 기준 -30%는 판정 전 구간에서 통계적으로 드물지 않다.
허용된 행동: 관측 계속, 이 문서 재독. 금지: 후보 제거, 파라미터 수정, 조기 판정.

**S2. 섀도가 크게 플러스다 — "판정 전에 조금만 실거래하고 싶다"**
→ 금지. 중간 성과로 진입하면 판정의 α가 무너진다 (좋아 보일 때만 진입하는
선택 편향). 실거래는 판정 통과 + 실전 투입 게이트 충족 후에만.

**S3. 신호가 몇 달째 없다 (트렌드 계열 등 저빈도 후보)**
→ 정상이다. 등록 시점에 3.2~5.3년 소요를 명시했다. 빈도를 높이는 파라미터
변경은 다중검정 오염. health의 신선도 감시가 정상이면 기다린다.

**S4. 데이터 공급이 끊겼다 (FDR/yfinance/분봉)**
→ 우선순위: 분봉(60일 지나면 영구 결손) > 아침 스냅샷·프리뷰(소급 불가) >
일봉(리플레이 소급 가능). ALERT.md가 뜨면 그 주 안에 복구. 복구 불가 시
결손 구간을 CLAUDE.md에 기록하고 판정 해석에 병기 — 표본은 삭제하지 않는다.

**S5. 판정 도달이 임박했고 결과가 아슬아슬해 보인다**
→ `judge` 한 번만 실행한다. 표본 경계는 '청산일 순 처음 N건'으로 동결돼 있어
실행 타이밍으로 결과를 고를 수 없다 — 그렇게 설계한 이유가 지금이다.

**S6. 판정이 실패했다 (계열 기각)**
→ 그 계열 종결이 결론이다. "아깝다, 규칙을 조금 바꾸면"은 지금까지의 모든
검증을 무효로 만든다. 허용: 새 메커니즘 가설의 사전 등록. 2.3년 관측이
아깝다는 매몰비용 논리는 기각 근거를 바꾸지 못한다.

**S7. 폭락이 왔다 (또는 진행 중이다)**
→ crash_study 결론: 폭락은 이 시스템의 주 손실원이 아니고, 진입 차단형
대책 2종은 이미 기각됐다. 새 필터 실험 금지. 관측은 폭락 중에도 계속.

**S8. 몇 달 자리를 비운다 (휴가·기기 고장)**
→ 일봉·장부는 소급 가능, 분봉은 60일 안에 한 번만 돌면 무손실. 프리뷰
증빙(ok 플래그)만 결손되는데 이는 진단용이라 판정 자체는 훼손 안 됨.
복귀 후: ALERT.md 확인 → `download` → `paper` → `health` 순.

**S9. 동결된 값이 실수로 바뀐 것을 발견했다**
→ git 이력에서 시점을 특정해 즉시 원복하고, 오염 구간(변경~원복)의 장부
행에 미치는 영향을 CLAUDE.md에 기록한다. 조용히 고치고 넘어가지 않는다.

**S10. 더 좋아 보이는 새 전략 아이디어가 생겼다**
→ 좋은 일이다. 단, 분기 1~2개 예산 안에서 사전 등록(PREREG 문서 + 실행 전
커밋) 절차로만. 기존 관측은 어떤 경우에도 건드리지 않는다.

## 원칙 한 줄 요약

**규칙을 바꿔도 되는 유일한 시점은 데이터를 보기 전이다.** 지금 이 문서를
쓰는 이유이며, 위 대응들이 전부 '지금' 정해진 이유다.
