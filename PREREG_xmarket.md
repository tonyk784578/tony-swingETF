# 사전 등록: xmarket — trend 계열 교차 시장 복제 (2026-08-26)

상태: **등록 v1 — 실행 전 커밋, 결과 미관측.** (overnight/flow 관례)

## 1. 목적

trend 계열(breakout/macross/trend_ride)은 홀드아웃(2009~2014 한국)에서
"판별 불가"였고 포워드 판정까지 약 2.3년 남았다. 동결된 전략 정의를
**다른 시장·다른 시대**에 그대로 적용하는 복제는 홀드아웃과 같은 성격의
재현 시험으로, 시험 수를 늘리지 않으면서 지금 얻을 수 있는 유일한 추가
독립 증거다. 질문: "이 추세추종 규칙들은 한국 2015+에만 맞는 우연인가,
일반 메커니즘인가."

## 2. 설계 (전 항목 고정 — 성과 미참조)

- **대상 시장/상품**: 미국 대표 지수 ETF 4종 — SPY(S&P500, 1993 상장),
  QQQ(나스닥100, 1999), DIA(다우30, 1998), IWM(러셀2000, 2000).
  선정 기준은 '역사 긴 대표 지수 상품'(universe_ext 관례) — 성과 미참조.
- **구간**: **2000-01-03 ~ 2014-12-31** (15년). 한국 인샘플(2015+)과 시대가
  겹치지 않아 글로벌 공통 레짐 오염을 차단하고, 한국 홀드아웃에 없던
  **2008 금융위기**와 닷컴 붕괴(2000~02)가 들어간다 — 관대한 시험이 아니다.
- **전략**: config `etf.strategies`의 breakout/macross/trend_ride 정의 그대로
  (파라미터·신호 함수 `raw_entry_signal`/`build_flags` 재사용 — 재구현 금지).
  exit 는 **기본형(ma)** — 채택된 trail 변형은 한국 후보에서 선택된 것이라
  다른 시장 복제에 실어 가면 그 선택 자체가 오버피팅 전파가 된다.
- **데이터**: yfinance `auto_adjust=False` 원가격 OHLC (분배금 미조정 —
  한국 인샘플 FDR 미조정가와 기준 일치. 미국 ETF 분기 배당 ~1~2%/년이
  가격에 낙폭으로 찍히므로 **전략에 불리한 방향의 편향** = 보수적).
  캐시 data/xmarket_{sym}.parquet (라이브 캐시와 분리).
- **비용**: 왕복 0.1% 유지 — 미국 실제(스프레드 1~2bp)보다 크게 보수적.

## 3. 판정 규칙 (결과 관측 전 확정)

12조합(4 ETF x 3전략)의 트레이드를 **계열 풀 1회 검정**: 평균>0 AND 단측
t>=1.66 (홀드아웃 trend 규칙과 동일). 조합별 표는 진단용 병기 — 조합
선별에 쓰지 않는다. 해석은 홀드아웃 3분법 그대로:

- 통과 → "추세추종 메커니즘이 다른 시장·시대에서도 재현 — 인샘플 근거 보강"
- 미달(평균>0) → "판별 불가 — 포워드 판정만이 답 (현상 유지)"
- 실패(평균<=0) → "재현 안 됨 — 포워드 판정 통과 시에도 반증 자료로 병기"

**어느 결과든 config 판정 기준·후보·파라미터 불변.** 시험 수 회계: 동결
규칙의 재현 시험이므로 declared_trials(160)에 가산하지 않는다 (홀드아웃 관례).

## 4. 금지

1회 실행. 구간·상품·비용·전략 변형 재실행 금지. 재실행은 리포트 재생성만.
